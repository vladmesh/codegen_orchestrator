# План рефакторинга графа нод

Итеративный план переработки архитектуры LangGraph для улучшения UX, масштабируемости и надёжности.

## Обзор изменений

```
Было:                           Станет:
user_message → PO → ...         telegram_buttons → API calls (быстро)
                                user_message → PO → Analyst (умно)
```

**Ключевые идеи:**
1. Telegram-кнопки для частых действий → прямые API-вызовы, без LLM
2. PO только для текстовых сообщений → делегирует Analyst'у сложные задачи
3. RAG-система для контекста → PO знает историю проектов
4. Спека проекта в репо → disaster recovery, source of truth

---

## Итерация 1: Telegram-кнопки (Intent Routing без LLM)

**Цель:** Быстрые действия через кнопки, LLM только для свободного текста.

### UI в Telegram

```
🏠 Главное меню
├── 📦 Список проектов
├── ➕ Новый проект  
└── 🖥️ Список серверов
```

При нажатии "Список проектов":
```
📦 Проекты:
├── weather-bot [active] → [Детали]
├── analytics-service [draft] → [Детали]
└── ← Назад
```

При нажатии "Детали":
```
📦 weather-bot
Status: active
Repo: github.com/user/weather-bot
Server: vps-123 (1.2.3.4:8080)

[🔧 Maintenance] [🚀 Deploy] [📊 Логи] [← Назад]
```

### Реализация

**Шаги:**
1. [ ] Добавить inline keyboard в `telegram_bot` сервис
2. [ ] Создать callback handlers для кнопок
3. [ ] Прямые вызовы API (`GET /projects`, `GET /servers`) без LLM
4. [ ] Роутинг: кнопка → API, текст → PO

**Файлы:**
- `services/telegram_bot/src/handlers/menu.py` — новые handlers
- `services/telegram_bot/src/keyboards.py` — inline keyboards

**Критерий готовности:** Пользователь может посмотреть проекты/серверы через кнопки без задержки LLM.

---

## Итерация 2: Разделение PO → PO + Analyst

**Цель:** PO — коммуникатор, Analyst — работа с проектами.

### Распределение ответственности

| Компонент | Роль | Tools |
|-----------|------|-------|
| **PO** | Общение с юзером, статусы, отчёты | `list_projects`, `get_project_status`, `list_incidents` |
| **Analyst** | Анализ требований, спеки, ТЗ | `create_project_spec`, `update_project_spec`, `validate_spec` |

### Логика роутинга PO → Analyst

```python
# В промпте PO:
"""
Если пользователь описывает НОВЫЙ ПРОЕКТ или ИЗМЕНЕНИЕ требований:
→ Передай задачу Analyst через tool delegate_to_analyst(task_description)

Если пользователь спрашивает о СТАТУСЕ, ПРОГРЕССЕ или ПРОБЛЕМАХ:
→ Ответь сам, используя RAG для контекста
"""
```

**Шаги:**
1. [ ] Создать `nodes/analyst.py` с LLMNode
2. [ ] Добавить tool `delegate_to_analyst` в PO
3. [ ] Промпт Analyst: анализ требований, генерация/изменение спеки
4. [ ] Роутинг в графе: `product_owner → analyst → spec_manager`
5. [ ] Убрать `brainstorm` — его функции переходят к Analyst

**Файлы:**
- `services/langgraph/src/nodes/analyst.py` — новая нода
- `services/langgraph/src/graph.py` — обновить роутинг

---

## Итерация 3: Спека проекта в репозитории

**Цель:** `.project-spec.yaml` как source of truth, disaster recovery.

### Формат спеки

```yaml
# .project-spec.yaml
version: "1.0"
project:
  name: "weather-bot"
  description: "Telegram bot для прогноза погоды"
  
modules:
  - telegram
  - weather_api
  
entry_points:
  - type: "telegram_bot"
    handlers: ["start", "weather", "settings"]

secrets_required:
  - TELEGRAM_BOT_TOKEN
  - OPENWEATHER_API_KEY

infrastructure:
  min_ram_mb: 256
  ports: [8080]
```

### Интеграция с существующими polling workers

**Расширение `github_sync.py`:**
```python
async def sync_project_spec(repo_full_name: str):
    """Читает .project-spec.yaml и обновляет БД."""
    content = await github_client.get_file_content(
        repo_full_name, ".project-spec.yaml"
    )
    if content:
        spec = yaml.safe_load(content)
        await api_client.update_project_spec(repo_full_name, spec)
```

**Шаги:**
1. [ ] Определить JSON Schema для `.project-spec.yaml`
2. [ ] Добавить `project_spec` поле в БД (JSONB)
3. [ ] Расширить `github_sync.py` — читать спеку при sync
4. [ ] Analyst генерирует спеку → коммитит в репо
5. [ ] При восстановлении из GitHub — спека восстанавливается автоматически

**Файлы:**
- `shared/schemas/project_spec.py` — Pydantic модель
- `services/scheduler/src/tasks/github_sync.py` — расширить
- `services/langgraph/src/tools/spec.py` — CRUD для спек

---

## Итерация 4: RAG-система для PO

**Цель:** PO имеет контекст о проектах, истории, решениях.

### Что индексируем

| Источник | Что индексировать | Обновление |
|----------|-------------------|------------|
| Репозитории | README, спека, структура | При push (webhook) |
| История | Коммиты, PR descriptions | При sync |
| Инциденты | Логи, решения | При закрытии инцидента |

### Архитектура

```
┌──────────────┐     ┌─────────────┐     ┌──────────────┐
│   PO Node    │────▶│  RAG Query  │────▶│   pgvector   │
└──────────────┘     └─────────────┘     └──────────────┘
                            │
                     ┌──────┴──────┐
                     │  Embedding  │
                     │   Service   │
                     └─────────────┘
```

**Шаги:**
1. [ ] Добавить pgvector extension в PostgreSQL
2. [ ] Создать таблицу `document_embeddings`
3. [ ] Embedding service (OpenAI или local)
4. [ ] Tool `search_context(query)` для PO
5. [ ] Indexer в scheduler — обновление при синках

**Файлы:**
- `alembic/versions/xxx_add_pgvector.py` — миграция
- `services/langgraph/src/tools/rag.py` — search tool
- `services/scheduler/src/tasks/indexer.py` — индексация

---

## Итерация 5: Интерактивные CLI-воркеры

**Цель:** CLI-агенты могут запрашивать уточнения у PO.

### Протокол

```python
# Worker → Orchestrator
class WorkerQuestion(WorkerEvent):
    event_type: Literal["question"]
    question: str
    context: dict  # что worker уже знает/сделал

# Orchestrator → Worker  
class WorkerAnswer(BaseModel):
    request_id: str
    answer: str
```

### Flow

```
Worker: "Какой формат API использовать — REST или GraphQL?"
   ↓ (Redis pubsub)
PO: получает вопрос, пытается ответить из RAG
   ↓
PO: (если не знает) → эскалация к юзеру в Telegram
   ↓
User: "REST с JSON"
   ↓
PO → Worker: answer
   ↓
Worker: продолжает работу
```

**Шаги:**
1. [ ] Определить Pydantic модели событий
2. [ ] Обновить `worker-spawner` — слушать `question` события
3. [ ] Роутинг вопросов в PO
4. [ ] Механизм эскалации к юзеру
5. [ ] Таймаут и fallback поведение

**Файлы:**
- `shared/schemas/worker_events.py` — расширить
- `services/worker-spawner/src/question_handler.py`
- `services/langgraph/src/nodes/product_owner.py` — handle questions

---

## Итерация 6: Cleanup и документация

**Цель:** Удалить устаревший код, обновить документацию.

**Шаги:**
1. [ ] Удалить `nodes/brainstorm.py` (заменён Analyst)
2. [ ] Обновить `ARCHITECTURE.md`
3. [ ] Обновить `docs/NODES.md`
4. [ ] Написать `docs/LIFECYCLE.md` — полный lifecycle проекта
5. [ ] Обновить README с новой архитектурой

---

## Приоритеты

| Итерация | Приоритет | Зависимости | Оценка |
|----------|-----------|-------------|--------|
| 1. Telegram-кнопки | 🔴 Высокий | Нет | 1-2 дня |
| 2. PO + Analyst | 🔴 Высокий | Нет | 2-3 дня |
| 3. Спека в репо | 🔴 Высокий | Итерация 2 | 2 дня |
| 4. RAG | 🟡 Средний | Итерация 3 | 3-4 дня |
| 5. Интерактивные CLI | 🟡 Средний | Итерация 2 | 2-3 дня |
| 6. Cleanup | 🟢 Низкий | Все выше | 1 день |

---

## Открытые вопросы

1. **pgvector vs external vector DB** — pgvector проще, но ограничен размером
2. **Embedding model** — OpenAI (ada-002) или local (sentence-transformers)?
3. **Таймаут на вопросы CLI** — 5 минут? Бесконечно?
4. **Формат ответов юзеру** — Markdown? Plain text?
