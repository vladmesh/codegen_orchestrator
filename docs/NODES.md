# Агенты

Каждый агент — это узел LangGraph с собственным набором инструментов и специализацией. Агенты могут вызывать друг друга нелинейно.

---

## 🧭 Product Owner (PO)

**Роль**: Центральный координатор. Классифицирует намерения пользователя и запускает нужные подсистемы.

**Когда вызывается**:
- При каждом новом сообщении пользователя (точка входа графа)
- Когда нужно получить статус проекта или запустить новую задачу

**Инструменты**:
- `list_projects` / `get_project_status` — получить список или статус проекта
- `create_project_intent` — инициировать новый проект без сбора деталей

**Выход**: Решение о следующем шаге (Brainstorm или ответ пользователю)

---

## 🧠 Брейнсторм

**Роль**: Первичный анализ запроса, генерация идей, уточнение требований.

**Когда вызывается**:
- При получении нового запроса от пользователя
- Когда другой агент сталкивается с неоднозначностью

**Инструменты**:
- Доступ к контексту предыдущих проектов
- Генерация вопросов для уточнения
- Формирование технического задания

**Выход**: Структурированное ТЗ для других агентов

---

## 📐 Архитектор

**Роль**: Проектирование структуры проекта, выбор модулей, создание спецификаций.

**Когда вызывается**:
- После получения ТЗ от Брейнсторма
- При необходимости изменить структуру проекта

**Инструменты**:
- `copier copy` — генерация нового проекта из service-template
- Редактирование YAML спецификаций (models, routers, events)
- Редактирование `services.yml`

**Выход**: Сгенерированный проект со спецификациями

---

## 👨‍💻 Разработчик

**Роль**: Написание бизнес-логики, контроллеров, обработчиков.

**Когда вызывается**:
- После генерации скелета проекта Архитектором
- При необходимости добавить/изменить функциональность

**Инструменты**:
- Редактирование Python кода в `controllers/`, `handlers/`
- Запуск `make generate-from-spec`
- Запуск `make lint`, `make tests`

**Выход**: Работающий код, проходящий линтеры и тесты

---

## 🧪 Тестировщик

**Роль**: E2E тестирование, проверка что всё работает как задумано.

**Когда вызывается**:
- После деплоя проекта на сервер
- При подозрении на регрессию

**Инструменты**:
- HTTP запросы к API
- Отправка команд Телеграм ботам
- Проверка health endpoints
- Чтение логов

**Выход**: Отчёт о тестировании (pass/fail + детали)

---

## 🔧 DevOps

**Роль**: Управление инфраструктурой, деплой, настройка серверов.

**Когда вызывается**:
- При необходимости развернуть новый проект
- При обновлении инфраструктуры

**Инструменты**:
- Ansible playbooks из prod_infra
- Редактирование `services.yml` для Caddy
- Управление Docker контейнерами
- Настройка DNS (через Cloudflare API)

**Выход**: Задеплоенный проект с работающим доменом

---

## 🏠 Завхоз (Resource Manager)

**Роль**: Управление ресурсами, выдача ключей, учёт.

**Когда вызывается**:
- При необходимости получить токен/ключ
- При выборе сервера для деплоя
- При регистрации нового ресурса

**Хранит**:
- Токены Телеграм ботов
- Ключи OpenAI, Anthropic
- Креды AWS, Cloudflare
- SSH ключи к серверам
- Список доменов и их статус
- Информация о серверах (нагрузка, доступное место)

**Инструменты**:
- CRUD для ресурсов
- Проверка доступности ресурса
- Автоматическая ротация ключей (TODO)
- Регистрация доменов (TODO)

**Выход**: Запрошенный ресурс или информация об ошибке

---

## 📝 Документатор

**Роль**: Ведение документации, генерация отчётов.

**Когда вызывается**:
- Периодически для создания сводки
- После завершения крупных задач

**Инструменты**:
- Чтение истории действий агентов
- Генерация Markdown отчётов
- Отправка отчётов в Телеграм

**Выход**: Отчёт о проделанной работе

---

## 🔄 Взаимодействие агентов

```
Пользователь (Телеграм)
    │
    ▼
Брейнсторм ──────► Архитектор
    │                  │
    │                  ▼
    │              Завхоз (токены, серверы)
    │                  │
    │                  ▼
    │              Разработчик
    │                  │
    │                  ▼
    │              DevOps ◄────► Завхоз (SSH, DNS)
    │                  │
    │                  ▼
    │              Тестировщик
    │                  │
    │                  ▼
    └──────────── Документатор ──► Пользователь (отчёт)
```

**Важно**: Это не строгий порядок. Любой агент может вызвать любого другого при необходимости.
