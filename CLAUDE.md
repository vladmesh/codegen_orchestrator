# Claude Code entrypoint

The canonical contributor and assistant instructions for this repository live in
[AGENTS.md](AGENTS.md). Read and follow that file before working here.

This compatibility entrypoint exists because Claude Code discovers `CLAUDE.md` automatically.
Do not add project rules here: update `AGENTS.md` so every coding agent receives the same guidance.
