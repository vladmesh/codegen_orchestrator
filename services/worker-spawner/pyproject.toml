[project]
name = "worker-spawner"
version = "0.1.0"
description = "Service for spawning coding worker containers"
requires-python = ">=3.12"

dependencies = [
    "redis>=5.0.0",
    "structlog>=25.1.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
