[defaults]
inventory = inventory/prod.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
stdout_callback = ansible.builtin.default
result_format = yaml

[privilege_escalation]
# Playbooks retain explicit become for standalone calls that use another config.
become = True
become_method = sudo
become_user = root
