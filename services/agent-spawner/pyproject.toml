[project]
name = "agent-spawner"
version = "0.1.0"
description = "Manages per-user CLI agent containers"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0.0",
    "structlog>=24.0.0",
    "pydantic-settings>=2.0.0",
]
