log_format json_log escape=json '{"level":"info","service":"user-dashboard",'
    '"remote_addr":"$remote_addr","request":"$request",'
    '"status":$status,"body_bytes_sent":$body_bytes_sent,'
    '"http_referer":"$http_referer","http_user_agent":"$http_user_agent",'
    '"request_time":$request_time}';

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    access_log /var/log/nginx/access.log json_log;
    error_log /var/log/nginx/error.log warn;

    # API proxy, avoids CORS. Scoped to the LK prefix on purpose: this server is
    # published to the internet through Caddy /lk/*, so anything proxied here is
    # public. The dashboard only ever calls /api/lk/* (see src/lib/api.ts and the
    # /api/lk/auth/token call in src/pages/AuthPage.tsx).
    location /api/lk/ {
        proxy_pass http://api:8000/api/lk/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Every other API route stays unreachable from the public dashboard origin.
    location /api/ {
        return 404;
    }

    # Health — no auth (for Docker healthcheck)
    location = /health {
        proxy_pass http://api:8000/health;
        proxy_set_header Host $host;
    }

    # SPA fallback — all non-file routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
