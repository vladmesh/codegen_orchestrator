[project]
name = "worker-broker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings",
    "redis",
    "structlog",
    # shared/ is copied as plain files rather than installed as a package.
    "httpx>=0.27.0",
    "pynacl>=1.5.0",
    "pyjwt[crypto]>=2.13.0",
    "cryptography>=43.0.0",
    "pgvector>=0.2.5",
    "pyyaml",
]

[tool.ruff]
line-length = 120
