networks:
  infra-test:
    name: infra-test-network
    driver: bridge

services:
  # Database
  db:
    image: pgvector/pgvector:0.8.6-pg16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    tmpfs: /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 5
    networks:
      - infra-test

  # Redis
  redis:
    image: redis:7.4.10-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 5s
      retries: 5
    networks:
      - infra-test

  # API Service
  api:
    image: codegen-orchestrator/api:test
    build:
      context: ../../..
      dockerfile: services/api/Dockerfile
      args:
        SOURCE_HASH: ${WORKER_SOURCE_HASH:-}
    command: ["/entrypoint.sh"]
    environment:
      - ENVIRONMENT=test
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/postgres
      - REDIS_URL=redis://redis:6379/0
      - LK_JWT_SECRET=test-lk-jwt-secret
      - INTERNAL_API_KEY=test-internal-api-key-for-integration-tests
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 2s
      timeout: 5s
      retries: 5
    networks:
      - infra-test

  # Seed the config read independently by both scheduler processes in this stack.
  system-config-seed:
    image: codegen-orchestrator/integration-test-runner:test
    build:
      context: ../../..
      dockerfile: tests/compose/integration/Dockerfile
      args:
        SOURCE_HASH: ${WORKER_SOURCE_HASH:-}
    environment:
      - INTERNAL_API_KEY=test-internal-api-key-for-integration-tests
    command:
      [
        "sh",
        "-c",
        "python /app/scripts/seed_system_configs.py --api-base-url http://api:8000 --configs-path /app/scripts/system_configs.yaml && touch /tmp/system-config-seeded && trap 'exit 0' TERM INT; sleep infinity & wait",
      ]
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "test", "-f", "/tmp/system-config-seeded"]
      interval: 2s
      timeout: 5s
      retries: 5
    networks:
      - infra-test

  scheduler-infrastructure:
    image: codegen-orchestrator/scheduler:test
    build:
      context: ../../..
      dockerfile: services/scheduler/Dockerfile
      args:
        SOURCE_HASH: ${WORKER_SOURCE_HASH:-}
    environment:
      - REDIS_URL=redis://redis:6379/0
      - API_BASE_URL=http://api:8000
      - GITHUB_ORG_NAME=test-org
      - TIME4VPS_USERNAME=test
      - TIME4VPS_PASSWORD=test
      - HEALTH_CHECK_INTERVAL=60
      - INTERNAL_API_KEY=test-internal-api-key-for-integration-tests
    command: ["python", "-m", "src.infrastructure"]
    depends_on:
      system-config-seed:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - infra-test

  scheduler-pipeline:
    image: codegen-orchestrator/scheduler:test
    environment:
      - REDIS_URL=redis://redis:6379/0
      - API_BASE_URL=http://api:8000
      - INTERNAL_API_KEY=test-internal-api-key-for-integration-tests
    command: ["python", "-m", "src.pipeline"]
    depends_on:
      system-config-seed:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - infra-test

  # Integration Test Runner
  integration-test-runner:
    image: codegen-orchestrator/integration-test-runner:test
    build:
      context: ../../..
      dockerfile: tests/compose/integration/Dockerfile
      args:
        SOURCE_HASH: ${WORKER_SOURCE_HASH:-}
    command: ["pytest", "tests/integration/infra/", "-v", "-x"]
    environment:
      - PYTHONUNBUFFERED=1
      - REDIS_URL=redis://redis:6379/0
      - API_BASE_URL=http://api:8000
      - INTERNAL_API_KEY=test-internal-api-key-for-integration-tests
    depends_on:
      scheduler-infrastructure:
        condition: service_started
      scheduler-pipeline:
        condition: service_started
      api:
        condition: service_healthy
    volumes:
      - ../../../tests:/app/tests
      - ../../../shared:/app/shared
    networks:
      - infra-test
