# E2E test secrets — copy to e2e-secrets.env and fill in real values.
# This file is gitignored. Create a test bot via @BotFather.
#
# Required for: tests with tg_bot module (#2 echo_bot, #4 weather_bot, #6 bot_landing, #7 expense_tracker)
# Required at:  Level C (deploy). Level A/B work without it.

TELEGRAM_BOT_TOKEN=
